{
    "version": "2.0.0",
    "tasks": [
        {
            "label":"echo",
            "type": "shell",
            "command":"kotlinc main.kt --include-runtime -d main.jar",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}